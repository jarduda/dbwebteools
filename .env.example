BOOTSTRAP_USERNAME=admin
# Set locally; do not commit your password.
BOOTSTRAP_PASSWORD=
# For localhost HTTP only, set Development. Production requires HTTPS at the reverse proxy.
APP_ENVIRONMENT=Production
